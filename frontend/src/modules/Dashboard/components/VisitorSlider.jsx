import UpdatesCarousel from '../Updates/UpdatesCarousel';

/** Legacy import path — home dashboard now uses the Updates carousel. */
const VisitorSlider = () => <UpdatesCarousel />;

export default VisitorSlider;
