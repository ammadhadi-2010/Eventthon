/**
 * Lottie CDN package URLs often return 403 when hotlinked.
 * Leave null to use BusinessIcon CSS fallback; set local /public paths when assets are hosted.
 */
export const BUSINESS_LOTTIE = {
  wallet: null,
  growth: null,
  network: null,
  analytics: null,
  success: null,
  rocket: null,
  target: null,
  briefcase: null,
};
